'use strict';

var React = require('react');
var BoxMeetup = require('./BoxMeetup');

var Boxes = React.createClass({
  render: function() {
    return (
      <BoxMeetup />
    );
  }
});

module.exports = Boxes;
